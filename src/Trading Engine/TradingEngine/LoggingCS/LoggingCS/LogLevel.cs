using System;

namespace TradingEngineServer.Logging
{
    // Let us create an enumeration that represents different levels of logging priority
    public enum LogLevel
    {
        Debug,
        Information,
        Warning,
        Error
    }
}
